PORT=5001

# The connection string from MongoDB Atlas (Cluster0 in Pune)
# Removed the < > from around your password
MONGO_URI=mongodb+srv://<username>:<password>@cluster0.pzpq6iq.mongodb.net/blog?retryWrites=true&w=majority
